//
//  LiveResize.swift
//  Aurora
//

import SwiftUI

extension View {
    /// Live resize snapshot mode is intentionally removed.
    /// Kept as a no-op for source compatibility.
    func bitmapLiveResize() -> some View {
        self
    }
}
